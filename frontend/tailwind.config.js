/** Minimal Tailwind v4 bridge config for tooling; theme tweaks live in `src/index.css` via `@theme`. */
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
}
